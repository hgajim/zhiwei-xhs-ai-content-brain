# 知微项目安全迁移说明

## 这份副本包含

- FastAPI 后端源代码
- Next.js / TypeScript 前端源代码
- PostgreSQL 与 pgvector 数据库迁移文件
- 自动化测试
- 项目说明、开发路线和 `AGENTS.md` 项目上下文
- `.env.example` 配置模板

## 已主动排除

- 真实 `.env` 配置
- 本地 API Key 加密密钥 `.local_encryption_key`
- Python 虚拟环境 `.venv`
- 前端 `node_modules` 与不完整依赖备份
- 编译产物、开发缓存和日志
- 本机上传的图片与其他 `data` 文件
- PostgreSQL 真实数据库内容

因此，这份文件夹可以安全复制，但复制后不会自动包含原电脑上的文章、知识、向量、账号、图片或模型密钥。

## 在另一台电脑恢复

1. 安装 PostgreSQL，并启用 `pgvector`。
2. 复制 `.env.example` 为 `.env`，填写新环境自己的数据库连接、Embedding 服务和模型配置。
3. 按编号执行 `migrations` 文件夹中的数据库迁移。
4. 根据根目录 `pyproject.toml` 安装后端依赖。
5. 根据 `frontend/package.json` 和 `frontend/pnpm-lock.yaml` 安装前端依赖。
6. 分别启动 FastAPI 后端、后台任务进程和前端网页。

## 如果还要迁移现有训练数据

当前安全副本故意不携带真实数据。需要另外制作 PostgreSQL 数据库备份，并单独复制图片资产目录；数据库备份可能包含账号、文章、知识、向量、模型设置和业务数据，不应与公开源码包混放。

## 安全提醒

- 不要把原项目的 `.env` 或 `.local_encryption_key` 直接发给他人。
- 新环境应使用新的数据库密码和新的应用加密密钥。
- 如果怀疑 API Key 曾经泄露，应在对应模型平台撤销旧 Key 并重新生成。
